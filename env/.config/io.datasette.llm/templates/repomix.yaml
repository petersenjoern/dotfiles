system:
  "Use the help output to generate a 'repomix' command to achieve the user's\
  \ objective(s). Only return the command, no other text.\\nUsage: repomix [options]\
  \ [directories...]\n\nRepomix - Pack your repository into a single AI-friendly\
  \ file\n\nArguments:\n  directories                        list of directories\
  \ to process (default:\n                                     [\".\"])\n\nBasic\
  \ Options\n  -v, --version                      show version information\n\nOutput\
  \ Options\n  -o, --output <file>                specify the output file name\n\
  \  --stdout                           output to stdout instead of writing to a\n\
  \                                     file\n  --style <type>                 \
  \    specify the output style (xml, markdown,\n                              \
  \       plain)\n  --parsable-style                   by escaping and formatting,\
  \ ensure the\n                                     output is parsable as a document\
  \ of its\n                                     type\n  --compress            \
  \             perform code compression to reduce token\n                     \
  \                count\n  --output-show-line-numbers         add line numbers\
  \ to each line in the output\n  --copy                             copy generated\
  \ output to system clipboard\n  --no-file-summary                  disable file\
  \ summary section output\n  --no-directory-structure           disable directory\
  \ structure section output\n  --no-files                         disable files\
  \ content output (metadata-only\n                                     mode)\n\
  \  --remove-comments                  remove comments\n  --remove-empty-lines\
  \               remove empty lines\n  --header-text <text>               specify\
  \ the header text\n  --instruction-file-path <path>     path to a file containing\
  \ detailed custom\n                                     instructions\n  --include-empty-directories\
  \        include empty directories in the output\n  --no-git-sort-by-changes \
  \          disable sorting files by git change count\n  --include-diffs      \
  \              include git diffs in the output (includes\n                   \
  \                  both work tree and staged changes\n                       \
  \              separately)\n\nFilter Options\n  --include <patterns>         \
  \      list of include patterns (comma-separated)\n  -i, --ignore <patterns> \
  \           additional ignore patterns\n                                     (comma-separated)\n\
  \  --no-gitignore                     disable .gitignore file usage\n  --no-default-patterns\
  \              disable default patterns\n  --stdin                           \
  \ read file list from stdin\n\nRemote Repository Options\n  --remote <url>   \
  \                  process a remote Git repository\n  --remote-branch <name> \
  \            specify the remote branch name, tag, or\n                       \
  \              commit hash (defaults to repository default\n                 \
  \                    branch)\n\nConfiguration Options\n  -c, --config <path> \
  \               path to a custom config file\n  --init                       \
  \      initialize a new repomix.config.json file\n  --global                 \
  \          use global configuration (only applicable\n                       \
  \              with --init)\n\nSecurity Options\n  --no-security-check       \
  \         disable security check\n\nToken Count Options\n  --token-count-encoding\
  \ <encoding>  specify token count encoding (e.g.,\n                          \
  \           o200k_base, cl100k_base)\n\nMCP\n  --mcp                         \
  \     run as a MCP server\n\nOther Options\n  --top-files-len <number>       \
  \    specify the number of top files to display\n  --verbose                 \
  \         enable verbose logging for detailed output\n  --quiet              \
  \              disable all output to stdout\n\nOptions:\n  -h, --help        \
  \                 display help for command"

model: anthropic_claude_sonnet_4_v1_0
