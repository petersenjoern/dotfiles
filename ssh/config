# Per-machine private hosts (tailnet FQDNs, identity files, etc.).
# `Include` silently no-ops when the file doesn't exist.
Include ~/.ssh/config.local

Host github.com
  IdentityAgent ~/.1password/agent.sock

Host github-work
  HostName github.com
  IdentityAgent ~/.1password/agent.sock
